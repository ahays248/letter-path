# Letter Path

Walk the path, pick letters left or right, spell the animal word.

Open the GitHub Pages URL for this repo (Settings → Pages, or the link printed after deploy).

Local: `python3 -m http.server 8080` then http://localhost:8080
